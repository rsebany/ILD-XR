# PostgreSQL
POSTGRES_PASSWORD=ild_xr_dev

# Backend API
ILD_JWT_SECRET=change-this-in-production
API_HOST=0.0.0.0
API_PORT=8000
# Keep 0 during AI uploads — reload kills in-flight Softmax ("Failed to fetch")
API_RELOAD=0
TORCH_NUM_THREADS=2
AI_FORCE_CPU=false
# Softmax — keep defaults for high-recall/precision triage (evaluation-metrics.json)
# ILD_INFER_MAX_PATCHES=8000
# ILD_INFER_CLEANUP_EVERY=64
ILD_INFER_FOLD=0
# ILD_HIERARCHICAL_WEIGHTS=weights/hierarchical_fold0.pth
# Lower if Windows TDR kills lungmask CUDA kernels ("launch timed out")
LUNGMASK_BATCH_SIZE=4

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
