"""Tests for backend-api service layer.

Layout mirrors ``services``:
  - ``ai`` — inference, mesh, preprocessing parity scripts
  - ``dicom`` — expert volume alignment
  - ``studies`` — expert mask comparison
"""
from __future__ import annotations
