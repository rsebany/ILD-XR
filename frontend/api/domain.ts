/**
 * Domain types aligned with `backend-api` (FastAPI) schemas.
 *
 * Re-exported from `./types` for a single entry point (e.g. `@/api/domain`).
 */
export type * from "./types";
export { ADMIN_USER_ROLES } from "./types/admin";
