"use client";

import { DarkFallbackRoom } from "./DarkFallbackRoom";

/** Standard dark procedural room background (hospital GLTF disabled). */
export function XrEnvironmentLayer() {
  return <DarkFallbackRoom />;
}
